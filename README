ncs is the nodecast server


INSTALL 

first, make some links in the externals directory :
cd externals
ln -s mongodb-src-r2.0.0 mongodb
ln -s qxmpp-0.3.0 qxmpp

If you prefer to use your system's libraries, you should modify the ncs.pro


second, make the mongodb client :
cd externals/mongodb
scons mongoclient

third, make the qxmpp client :
cd externals/qxmpp
qmake
make

four, install the libqxt library 
cd externals/libqxt-v0.6.1
qmake
make
make install

last, make the nodecast server :
qmake
make


USE
ncs --mongodb-ip=127.0.0.1 --mongodb-base=nodecast_prod --domain-name=(localhost|external IP|your-domain-name.tld) --xmpp-client-port 6222  --xmpp-server-port 6269

MONGODB
create user collection
doc = { login : 'ubic', email : 'ubic@pumit.com', authentication_token : '2PxKTkN2heXm'}
db.users.insert(doc);


API USE
1. register server
curl -H "X-nodename: your-node-name" --user "email:token" http://127.0.0.1:4567/node/create

2. push data
curl -H "X-task: taskname" -H "X-uuid: your-node-uuid" -H "X-payloadfilename: filename" -X POST --data-binary @filename --user "email:token" http://127.0.0.1:4567/payload/create



MONGODB
create index :
db.process_statistics.getIndexKeys();
db.process_statistics.ensureIndex({host_id: 1, created_at: -1});






contact : fredix at nodecast dot net

